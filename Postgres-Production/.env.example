# =============================================================================
# PostgreSQL Production Configuration
# =============================================================================

## PostgreSQL Superuser Credentials
## Username: Random UUID (no hyphens, lowercase)
## Password: SHA-512 hash for maximum security
POSTGRES_USER=
POSTGRES_PASSWORD=

## PostgreSQL Port (custom port for security)
POSTGRES_PORT=

# =============================================================================
# Database Creation
# =============================================================================
# PostgreSQL starts with only the default 'postgres' database
# Create additional databases using: make create-db
